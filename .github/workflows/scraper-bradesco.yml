# ===== scraper-bradesco.yml =====
name: Bradesco

on:
  schedule:
    - cron: '0 2 5 * *'  # Dia 5 de cada mês às 2h UTC
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 75  # 1h15min (script tem timeout interno de 1h)
    
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Cache Playwright
        uses: actions/cache@v3
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            playwright-${{ runner.os }}-
      
      - name: Instalar deps
        run: |
          pip install playwright requests beautifulsoup4
          playwright install chromium --with-deps
      
      - name: Rodar scraper
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: |
          cd scrapers
          python bradesco_scraper.py --full-update
      
      - name: Upload logs em caso de erro
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: bradesco-error-logs-${{ github.run_number }}
          path: scrapers/bradesco_data/*.json
          retention-days: 7