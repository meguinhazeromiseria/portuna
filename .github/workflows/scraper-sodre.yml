# ===== scraper-sodre.yml =====
# SODRÉ SANTORO - Todas as 5 categorias de uma vez
# Execução: 7:05 UTC (04:05 BRT - 40min após Caixa)
# Timeout: 180min (3h, script tem timeout interno de 2h58min)

name: Sodré Santoro - Full Update

on:
  schedule:
    - cron: '5 7 * * *'  # TODO DIA às 7:05 UTC = 04:05 BRT
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 180  # 3h
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Cache Playwright
        uses: actions/cache@v3
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            playwright-${{ runner.os }}-
      
      - name: Install Dependencies
        run: |
          pip install playwright requests
          playwright install chromium --with-deps
      
      - name: Run Sodré Scraper
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          PYTHONUNBUFFERED: "1"
        run: |
          cd scrapers
          
          echo "======================================"
          echo "🔨 SODRÉ SANTORO - FULL UPDATE"
          echo "======================================"
          echo "📅 Início: $(date -u '+%Y-%m-%d %H:%M:%S UTC')"
          echo "🇧🇷 Horário Brasília: $(TZ='America/Sao_Paulo' date '+%Y-%m-%d %H:%M:%S BRT')"
          echo "📦 Processando 5 categorias"
          echo "⏱️  Timeout: 2h58min"
          echo "======================================"
          echo ""
          
          START=$(date +%s)
          
          python -u sodre_scraper.py --full-update
          
          EXIT_CODE=$?
          END=$(date +%s)
          DURATION=$(( (END - START) / 60 ))
          
          echo ""
          echo "======================================"
          if [ $EXIT_CODE -eq 0 ]; then
            echo "✅ SODRÉ CONCLUÍDO em ${DURATION} minutos"
          else
            echo "❌ SODRÉ FALHOU (código $EXIT_CODE) após ${DURATION} minutos"
          fi
          echo "🕐 Término UTC: $(date -u '+%Y-%m-%d %H:%M:%S UTC')"
          echo "🇧🇷 Término BRT: $(TZ='America/Sao_Paulo' date '+%Y-%m-%d %H:%M:%S BRT')"
          echo "======================================"
          
          exit $EXIT_CODE
      
      - name: Upload Artifacts on Failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: sodre-error-logs-${{ github.run_number }}
          path: scrapers/sodre_data/*.json
          retention-days: 7
      
      - name: Summary
        if: always()
        run: |
          echo "## 🔨 Sodré Santoro - Full Update" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Status:** ${{ job.status }}" >> $GITHUB_STEP_SUMMARY
          echo "**Horário UTC:** $(date -u '+%Y-%m-%d %H:%M')" >> $GITHUB_STEP_SUMMARY
          echo "**Horário BRT:** $(TZ='America/Sao_Paulo' date '+%Y-%m-%d %H:%M')" >> $GITHUB_STEP_SUMMARY
          echo "**Categorias:** Todas (5)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ "${{ job.status }}" = "success" ]; then
            echo "✅ Scraping concluído!" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "📊 **Desempenho:**" >> $GITHUB_STEP_SUMMARY
            echo "- ⚡ ~4.700 lotes em ~5 minutos" >> $GITHUB_STEP_SUMMARY
            echo "- 🔥 ~940 lotes/minuto" >> $GITHUB_STEP_SUMMARY
          else
            echo "❌ Falhou — veja os logs para detalhes." >> $GITHUB_STEP_SUMMARY
          fi